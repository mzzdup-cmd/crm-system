# TT Sheets Sync (GitHub Actions / local script)
GOOGLE_SERVICE_ACCOUNT_JSON=
FIREBASE_PROJECT_ID=crm-school-v2

# Optional: override all manager TT tables (JSON). Default: managerTtSpreadsheets.json in repo.
# MANAGER_TT_SPREADSHEETS_JSON=

# Tab name if not "TT" / "ТТ" (auto-detected)
TT_SHEET_TAB=TT

# Legacy MM full replace (disabled)
# SHEETS_SPREADSHEET_ID=
